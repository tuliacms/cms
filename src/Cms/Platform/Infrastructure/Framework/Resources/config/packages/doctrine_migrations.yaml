doctrine_migrations:
    storage:
        table_storage:
            table_name: '%env(default::DATABASE_PREFIX)%migration_versions'
    migrations_paths:
        'Tulia\Cms\Migrations': '%cms.core_dir%/Migrations'
    enable_profiler: '%kernel.debug%'
